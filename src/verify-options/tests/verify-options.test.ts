import { describe, expect, it } from 'vitest';

import verifyOptions from '../index.js';

describe('verifyOptions', () => {
  it.todo('CHANGEME: add tests for semantic validation');

  it('is a function', () => {
    expect(verifyOptions).toBeInstanceOf(Function);
  });
});

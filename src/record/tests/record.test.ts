import { describe, expect, it } from 'vitest';

import record from '../index.js';

describe('record', () => {
  it.todo('CHANGEME: add tests for the record function');

  it('is a function', () => {
    expect(record).toBeInstanceOf(Function);
  });
});

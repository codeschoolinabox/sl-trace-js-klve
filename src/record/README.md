# record

Implements the `RecordFunction` contract from `@study-lenses/tracing`.

## Files

- `index.ts` — entry point; adapts the tracer engine to the RecordFunction interface
- `types.ts` — `TracerOptions` and `TracerStep` type definitions
- `tests/` — unit tests for the record function
